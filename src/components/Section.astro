---

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section class="flex-col justify-center ">
    <h1 class="text-3xl mx-auto justify-center flex font-semibold text-dark my-14">{title}</h1>
    <slot />
</section>